import styled from 'styled-components';
import img from './src/footer-bg.jpg';

export const AppHeader = styled.header`
    background-size: cover;
    background-image: url(${img});
    background-position: center center;
`;